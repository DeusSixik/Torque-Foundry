Тех док по реализации физики в моде Torque Foundry
(статусы: ГОТОВО — работает в коде, ЧАСТЬ — база есть, ПЛАН — будущее)

> WARNING
> Все физические вычисления должны быть максимально реалистичными. Есть
> случаи, когда физические формулы крайне тяжелы в просчётах — тогда их
> можно упрощать, но так, чтобы окончательный результат не имел разброса
> в показателях больше чем 10-15%.

> ЕДИНИЦЫ (правило без исключений)
> Игровые спеки и публичные API — в СИ (RPM, Nm, кг, Дж, °C).
> Внутренние буферы движка — в целых milli-единицах (milli-RPM, milli-Nm),
> конверсия только на границе. Не смешивать в одной структуре.

## Базовые компоненты

Начало физики берёт от материалов — они управляют основными показателями
(ГОТОВО: record PhysicsMaterial, 14 полей, 4 группы):
1. Масса и инерция: плотность (kg/m³) — масса детали по объёму, инерция сети
2. Упругость/жёсткость: модуль сдвига G, модуль Юнга E, коэффициент Пуассона ν
3. Пределы прочности: текучести (σ_y растяжение, τ_y сдвиг), временное
   сопротивление σ_u, усталостная прочность σ-1, твёрдость HB
4. Трение и теплофизика: коэффициент трения μ, тепловое расширение α,
   теплоёмкость c, теплопроводность λ

### Философия данных: независимое задаём, зависимое выводим (ГОТОВО)

В материале задаются ТОЛЬКО независимые физические константы. Зависимые
выводятся автоматически по формулам сопромата (Builder):
- G = E / (2(1+ν))                      [оверрайд для анизотропии — дерево]
- τ_y = σ_y / √3                        (критерий текучести фон Мизеса)
- σ-1 = k_fatigue · σ_u                 (эмпирическая оценка выносливости;
  k_fatigue — КОЭФФИЦИЕНТ СЕМЕЙСТВА материалов: сталь 0.45, чугун ~0.4,
  бронза ~0.35, дерево ~0.35. Соотношение 0.45 валидно только для сталей:
  у алюминия настоящего предела выносливости нет вообще (кривая усталости
  не выходит на горизонталь), у меди/дерева коэффициент другой. Хардкод
  одной константы на все семейства при CALPHAD-интерполяции даст системную
  ошибку — поэтому коэффициент наследуется от архетипа и переопределяется.)
Игровая динамика тоже не хранит отдельных полей, а потребляет физику через
производные методы: инерция сети = ρ/7850×2, трение сети = μ/15,
безопасные обороты = 256·√(σ_y/σ_y_стали)  [Допущение — см. ниже].
Плюс: поменял физику — игровой баланс сдвинулся сам, рассинхрон невозможен.
Архетипы (steel/wood/bronze/castIron) задают типовые константы семейства —
новый материал = 3-4 цифры.

### Допущение: безопасные обороты без плотности (сознательное упрощение)

Реальный физический потолок вращения — центробежный разрыв: напряжение
σ ∝ ρω²r², откуда ω_max ∝ √(σ_y/ρ). Наша формула нормирует только прочность,
без плотности. Разлад: дерево лёгкое (центробежная нагрузка мала), и по
«чистой» физике могло бы крутиться быстрее стали. Плотность опущена
СОЗНАТЕЛЬНО: игровой износ моделирует поверхностную усталость и износ
опор, а не разрыв обода; на игровых радиусах центробежный разрыв недостижим.
Последствия: относительный порядок материалов (дерево < чугун < бронза <
сталь) сохранён, абсолютные числа — игровые. Когда появится геометрия
деталей, добавить второй лимит: burstRPM(r) ∝ √(σ_y/ρ)·1/r и брать
min(прочностной, центробежный). До тех пор формула — игровой баланс, а не
физический предел (маркировать при любых правках).

### Металлургия сплавов CALPHAD (ПЛАН)

Материалы должны быть модульными и лёгкими в настройках: в будущем
металлургия сплавов по пропорциям через CALPHAD (расчёт термодинамических
диаграмм фаз) позволит создавать сплавы динамически. Схема:
игрок задаёт пропорции компонентов -> физические свойства ИНТЕРПОЛИРУЮТСЯ
(плотность, E, σ_y, μ, c...) -> всё зависимое выводится существующим
Builder'ом автоматически -> PhysicsMaterial собирается на лету.
Требование к коду: материал — иммутабельные данные, никаких ссылок на
зарегистрированные пресеты внутри машин (уже соблюдается).

Для выбора сплавов у механизмов будут требования-спеки: «этому узлу нужно
σ_u >= N, λ >= A» — по ним игрок решает, какой сплав где применять (ПЛАН).

## Механические цепочки

### Шасси (коробка)

Базовая коробка — шасси, по умолчанию пустая: игрок сам устанавливает
нужный вал и шестерни.

### Кастомные шестерни (ПЛАН, упрощённая модель)

Мод должен понимать следующие параметры шестерни:
- Передаточное число (главный играбельный параметр)
- Изгибная прочность зуба -> рейтинг момента
- Контактная выносливость поверхностей -> рейтинг оборотов
- Форма зубьев (визуальная модель по шаблону)
[ОТЛОЖЕНО: модуль зацепления, эвольвентный профиль, угол зацепления —
игрок их не увидит; весь расчёт сведён к рейтингам момента/оборотов.
Изнашиваются все зубья сразу, не по одному — по одному дорого.]

### Валы (ЧАСТЬ)

- Предел крутящего момента: T_max = (π r³/2) · τ_y / k (ГОТОВО, r=0.025, k=2)
- Скоростной лимит: износ выше безопасных оборотов материала (ГОТОВО)
- Собственная инерция: пока константа материала; далее ½mr² по геометрии (ЧАСТЬ)
- Нагрев (ГОТОВО: трение -> тепло)
- Центровка (ПЛАН): перекос -> множитель трения + ускоренный износ;
  настройка руками или станками
- Длина пролёта без опор (ПЛАН): непрерывные цепочки валов для обслуживания

### Опоры и смазки (ПЛАН, реализация — модификаторы)

Валы устанавливаются в шасси — коробка даёт базовый упор. Для сложных
цепочек игрок улучшает опоры:
1. Опоры (очень малые скорости)
2. Втулки (огромные радиальные нагрузки), < 500 RPM
3. Радиальные шарикоподшипники (2000+ RPM, закрытые, без ухода)
4. Роликовые цилиндрические (1000-1500 RPM, периодическая замена смазки)
5. Конические/упорные (500-1500 RPM, масляная ванна)

ВАЖНО: подшипник и смазка — это МОДИФИКАТОРЫ узла, а не отдельная физика:
- смазка -> множитель μ узла и множитель предельной температуры
- несоответствие RPM-рейтингу -> множитель нагрева и износа
Таблица смазок (предварительно, может измениться):

Тип смазки,Оптимальные RPM,"Предельная нагрузка (T, F_N)",Рабочая температура,Конструктивное требование
"Твёрдая (графит, MoS2)",Низкие (<150),Экстремальная,До +500 C,"Прямое нанесение, сухой узел"
"Пластичная (литол, солидол)",Средние (100-1500),Высокая,До +70...120 C,"Закрытый пыльник, замена шприцем"
Жидкая минеральная,Высокие (1000-6000),Средняя,До +90 C,"Картер, ванна или капельная маслёнка"
Жидкая синтетическая,Экстремальные (3000-15000),Высокая,До +220 C,Принудительный контур с охлаждением
Газовая,Ультра (>20000),Мизерная,Любая,"Компрессор постоянного поддува (пасхалка, под неё движок не проектируем)"

## Механизмы

Спеки механизма (все в СИ; в движок — конверсия на границе):

public record MachineOperationalSpec(
    // Силовые пороги
    double minBreakawayTorqueNm, // Момент страгивания: ниже — машина не
                                 // стронется и заклинит сеть (ПЛАН)
    double minSpeedRpm,          // Минимальные обороты полезного действия (ГОТОВО: required)

    // Инерция и холостой ход
    double rotorInertiaKgM2,     // Инерция рабочего барабана/ротора (ПЛАН,
                                 // добавляется к инерции сети)
    double idleTorqueNm,         // Потери холостого хода (ПЛАН, в loadTorque)

    // Тепло и КПД
    double efficiency,           // КПД: P_loss = P_in * (1 - eta) -> в тепло
                                 // корпуса машины (ПЛАН, приоритет 1)
    double maxTemperatureC,      // Порог деградации (ПЛАН)

    // Монтаж (дешёвая версия)
    double maxShaftMisalignmentDeg // Перекос: множитель трения+износа (ПЛАН)
) {}

[ОТЛОЖЕНО: torqueRippleFactor и vibrationEmissionRate — вибрации без
рендера/звука неощутимы; вернуться, когда появится на что смотреть.]

### Генераторы

- Выходные параметры: номинальные обороты/момент/мощность, предельные
  обороты (ГОТОВО: 256 RPM / 64 Nm базовый)
- Поведение под нагрузкой: пусковой момент, опрокидывающий (пиковый)
  момент, инерция ротора (ПЛАН)
- Тепловые ограничения: КПД, рассеивание, предельная температура (ГОТОВО:
  GeneratorMachine — потери P_out·(1-η)/η греют ротор; выше предельной
  температуры момент линейно derate'ится до пола 0.3)
- Режим работы S1-S8 — ГОТОВО как эмерджентное поведение: НЕ таймерами,
  режим ВОЗНИКАЕТ из тепловой модели. Перегруз -> нагрев ротора
  (теплоёмкость поглощает пик = S2) -> при предельной температуре derate
  момента -> потребитель клинит -> скорость 0 -> потерь нет -> остывание
  -> восстановление. Тяга сети считается от DERATED момента (фаза B берёт
  вход фазы A, не паспорт) — иначе перегрев не останавливает нагрев.
- Радиальные/осевые нагрузки на вал (ПЛАН, вместе с опорами)

### Передачи (ГОТОВО — все как шаблоны)

- Коническая передача
- Крестовой конический разветвитель
- Механический дифференциал
- Маховик (энергобуфер)
- Коробка передач
- Планетарный механизм
- Раздаточная коробка
- Червячная передача (односторонняя, самотормозящая — не разветвитель)

Идеал — сборка авторских коробок игроком: «сборочная коробка» как
виртуальный узел (см. движок). Пока шаблоны полностью закрывают геймплей.

## Физика сети (ГОТОВО — фиксированные правила, не менять без обсуждения)

### Сеть — маховик

Все механизмы вплотную образуют сеть. Динамика сети — ОДНА скорость на
всю сеть (жёсткая сцепка): spin-up/плато/выбег считаются для скаляра
оборотов. Момент источника разгоняет сеть, трение тормозит:
- Разгон: инерция = сумма инерций всех ВРАЩАЮЩИХСЯ машин
- Равновесие: тяга источника = суммарному трению (f·RPM на машину) —
  длинная цепь «съедает» обороты (8 железных валов: 256 -> ~237 RPM)
- Выбег: после снятия источника сеть плавно останавливается трением
- Мёртвые ветви (за потребителем, куда мощность не доходит) не вращаются
  и сеть НЕ нагружают

[ОГРАНИЧЕНИЕ МОДЕЛИ — инерция без передаточных чисел. transform() на
ребре меняет передаваемую мощность (после КПП 1:2 на ребре видно
512 RPM / 16 Nm), но динамика сети — один скаляр: жёсткая сеть с КПП
физически противоречива. Правильная модель — ПОСТРОЧНЫЕ скорости ветвей
и приведённая инерция (reflected inertia): инерция поддерева за узлом
домножается на ratio² узла (энергия ½Iω², а не сам I) — тяжёлый барабан
за редуктором 1:10 ощущается источником в 100 раз тяжелее. Требует
перехода от скаляра сети к скоростям по рёбрам — большой отдельный кусок,
см. приоритеты. До тех пор КПП меняет только раскладку момента/оборотов
на рёбрах, не динамику.]

### Перегруз: три случая (не смешивать!)

1. ЖЁСТКАЯ ПЕРЕГРУЗКА — суммарный требуемый момент потребителей больше,
   чем источники дают в принципе. Мгновенный стоп + КЛИН всей цепи до
   источника. Заклинивший потребитель давит на сеть (статическое трение),
   не давая раскрутиться обратно. Клин держится, пока перегруз не уйдёт.
2. МАРГИНАЛЬНЫЙ ДЕФИЦИТ — момент потребителя посильный, но трение на
   высоких оборотах съело запас. Сеть НЕ встаёт: плавное торможение,
   потребитель отключается, разгон — устойчивая «пила» у порога включения.
   Мгновенное обнуление здесь давало вечный цикл разгон->порог->ноль (баг
   был, починен — не возвращать).
3. КОНФЛИКТ НАПРАВЛЕНИЙ (ГОТОВО): слияние допускает
   несколько родителей. Если на один узел сходятся источники с РАЗНЫМ
   направлением вращения — их моменты гасят друг друга (энергия уходит в
   тепло/клин), а не складываются. Класс — жёсткая перегрузка (третий
   триггер клина). Знак-учёт в ДВУХ местах: слияние inputPower (Σ signed)
   и тяга сети в фазе B (опорное направление = первый источник).
   Заклинившая машина давит СТРАГИВАНИЕМ, а не рабочим моментом.

   РЕАЛИЗАЦИЯ (обязательно, не изобретать заново): знак-учёт —
   Σ signed_torque по родителям узла (одно направление +, встречное −).
   Риск того же класса дребезга, что был в маргинальном дефиците: при
   близких к равновесию встречных источниках net_torque ≈ 0, и
   remainder-округление качает его вокруг нуля тик от тика — получим
   флип клин<->не-клин каждый тик. Лечение то же, что уже стоит в
   маргинальном дефиците: переключение состояния только после N тиков
   подряд в допуске (гистерезис), не по одиночному тику. Переиспользовать
   один механизм гистерезиса на оба случая.

### Износ валов

Теряется МОМЕНТ (КПД), не обороты:
- Обороты выше безопасного лимита материала: ~1% x (превышение/лимит)
  на каждый вал пути
- Момент выше T_max: пластические потери; двукратное превышение — срез

### Нагрев

- Трение -> тепло: P = τ_тр · ω, за тик E += P/20 (железный вал на
  256 RPM ~ 200 Вт)
- Температура: T = окружающая + E / (m·c); масса — номинальная деталь
  1000 см³, пока у машин нет геометрии (сталь 7.85 кг, дерево 0.7)
- Конвекция к среде: постоянная времени τ = c / k_охл, где
  k_охл = 8 Вт/(кг·К) — конвекция на килограмм массы. Размерность сходится:
  [Дж/(кг·К)] / [Вт/(кг·К)] = [Дж/Вт] = секунды (сталь ~58 с, дерево ~212 с —
  теплоёмкие остывают дольше, как в жизни). Допущение: k_охл прячет в себе
  отношение площади теплообмена к массе (A/m) эталонной детали 1000 см³ —
  для деталей сильно иной формы реальная конвекция уйдёт за допуск 10-15%.
  Уточнится автоматически, когда в модель войдёт геометрия (тот же пакет,
  что инерция по геометрии).
- Тепло живёт в машине (physics.machine.SimulationState): на нём строятся деградация
  от перегрева, передача тепла между машинами, нагрев помещений
- КПД механизмов (будущий): P_loss = P_in(1-η) льётся в тот же addHeatJ

## Физический движок

Движок работает в ОДНОМ выделенном потоке, опережая тик сервера (кадровая
логика читает вчерашний результат — стабильно и без блокировок).
Разбивка групп по разным потокам — задел на будущее; имеет смысл только
при сотнях групп (закон Амдала: синхронизация съест выигрыш на мелких).

Механизмы группируются в группы по механической связности.

Группа — НАПРАВЛЕННЫЙ АЦИКЛИЧЕСКИЙ ГРАФ (DAG), не дерево: узлы могут иметь
несколько родителей (слияние от двух генераторов) и несколько детей.
Уровневая фильтрация режет обратные рёбра. Узлы изменения поведения —
transform() на каждой машине: после КПП одна ветка получит 512 RPM / 16 Nm,
другая останется 256 RPM / 32 Nm.

Сборочные коробки (ПЛАН): коробка, собранная игроком из валов и шестерён,
для движка — виртуальный узел: подграф с одной точкой входа и выхода,
считается как одна машина с агрегированным transform(). Начинать с шаблонов
(уже есть и работают), виртуальный узел — большой отдельный кусок.

## Приоритеты (дёшево -> дорого, каждый оживляет готовое)

1. КПД: transform() теряет (1-η) -> addHeatJ — связывает передачи с нагревом (ГОТОВО:
   getEfficiency() на машине, applyEfficiency в фазе A; червячная 0.75,
   ремень 0.92, раздатка 0.94, коническая 0.96, КПП/крест 0.95, планетар/дифф 0.97)
2. Конфликт направлений при слиянии: встречные источники гасят момент
   (знак-учёт в слиянии и в тяге сети) + гистерезис клина N=20 тиков (ГОТОВО)
3. Момент страгивания: getBreakawayTorqueRaw(), фаза C INSUFFICIENT + клин;
   потребитель страгивается своим рабочим моментом (ГОТОВО)
4. Холостой ход: getIdleTorqueRaw() в loadTorque при вращении (ГОТОВО, механизм;
   пресеты машин — по мере баланса)
5. Инерция ротора: getExtraInertia() + к инерции сети (ГОТОВО, механизм)
6. Тепловой derate генератора: onSourceTick греет ротор потерями,
   getOutputFactor() режет паспортный момент у предела температуры;
   duty cycle S1-S8 — эмерджентно (ГОТОВО)
7. Подшипники/смазки как модификаторы узла
8. Центровка (множители трения/износа)
9. Кастомные шестерни (передаточное число + рейтинги)
10. Сборочные коробки (виртуальный узел)
11. Построчные скорости ветвей + приведённая инерция (ratio²) — снимает
    противоречие «жёсткая сеть с КПП», добавляет геометрию деталей
    (реальная инерция ½mr², конвекция с A/m)
12. CALPHAD-сплавы (интерполяция свойств + per-family коэффициенты +
    существующий Builder)
